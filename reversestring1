 #include <bits/stdc++.h>
using namespace std;
//function for reverse string
void reverse(string str)
{
    for (int i=str.length()-1; i>=0; i--)
    cout<<str[i];
}
//function for string to be reversed
int main(void)
{
    string x= "just_a_beginner";
    reverse(x);
    return(0);
}
